<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <title>Profil</title>
</head>
<body>
  <h1>Dein Profil</h1>

  <!-- Profilbild anzeigen -->
  <% if (user.image_url) { %>
    <img src="<%= user.image_url %>" alt="Profilbild" style="width: 150px; height: 150px;">
  <% } else { %>
    <p>Kein Profilbild vorhanden</p>
  <% } %>

  <!-- Profildaten anzeigen -->
  <p><strong>Name:</strong> <%= user.name %></p>
  <p><strong>Geschlecht:</strong> <%= user.gender %></p>
  <p><strong>Geburtstag:</strong> <%= user.birthday %></p>

  <!-- Formular zum Bearbeiten der Profildaten -->
  <h2>Profildaten bearbeiten</h2>
  <form action="/profil/update" method="POST">
    <label for="name">Name:</label>
    <input type="text" id="name" name="name" value="<%= user.name %>" required>
    <br>
    <label for="gender">Geschlecht:</label>
    <select id="gender" name="gender">
      <option value="male" <%= user.gender === 'male' ? 'selected' : '' %>>Männlich</option>
      <option value="female" <%= user.gender === 'female' ? 'selected' : '' %>>Weiblich</option>
    </select>
    <br>
    <label for="birthday">Geburtstag:</label>
    <input type="date" id="birthday" name="birthday" value="<%= user.birthday %>" required>
    <br>
    <button type="submit">Speichern</button>
  </form>
</body>
</html>