<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Filter</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <!-- Navbar einfügen -->
    <%- include('partials/navbar') %>

    <div class="container mt-5">
        <h1 class="text-center">Filtereinstellungen</h1>
        <form action="/filter/update" method="POST" class="mt-4">
            <div class="mb-3">
                <label for="gender" class="form-label">Geschlecht:</label>
                <select id="gender" name="gender" class="form-select">
                    <option value="male">Männlich</option>
                    <option value="female">Weiblich</option>
                </select>
            </div>
            <div class="mb-3">
                <label for="minAge" class="form-label">Mindestalter:</label>
                <input type="number" id="minAge" name="minAge" class="form-control" required>
            </div>
            <div class="mb-3">
                <label for="maxAge" class="form-label">Maximalalter:</label>
                <input type="number" id="maxAge" name="maxAge" class="form-control" required>
            </div>
            <button type="submit" class="btn btn-primary w-100">Filter speichern</button>
        </form>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>