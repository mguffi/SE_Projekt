<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profil</title>
</head>
<body>
    <h1>Profilseite</h1>
    <div>
        <h2>Deine Profildaten</h2>
        <img src="<%= profile.profilePicture %>" alt="Profilbild" style="width: 150px; height: 150px;">
        <p><strong>Name:</strong> <%= profile.name %></p>
        <p><strong>Geschlecht:</strong> <%= profile.gender %></p>
        <p><strong>Geburtstag:</strong> <%= profile.birthday %></p>
    </div>

    <div>
        <h2>Profildaten bearbeiten</h2>
        <form action="/profil/update" method="POST">
            <label for="name">Name:</label>
            <input type="text" id="name" name="name" value="<%= profile.name %>"><br><br>

            <label for="gender">Geschlecht:</label>
            <select id="gender" name="gender">
                <option value="Male" <%= profile.gender === 'Male' ? 'selected' : '' %>>Male</option>
                <option value="Female" <%= profile.gender === 'Female' ? 'selected' : '' %>>Female</option>
                <option value="Other" <%= profile.gender === 'Other' ? 'selected' : '' %>>Other</option>
            </select><br><br>

            <label for="birthday">Geburtstag:</label>
            <input type="date" id="birthday" name="birthday" value="<%= profile.birthday %>"><br><br>

            <button type="submit">Speichern</button>
        </form>
    </div>
</body>
</html>