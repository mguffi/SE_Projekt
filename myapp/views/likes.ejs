<ul>
    <% matches.forEach(match => { %>
        <li>
            <img src="<%= match.image_url %>" alt="Profilbild" style="width: 50px; height: 50px;">
            <strong><%= match.name %></strong>
        </li>
    <% }) %>
</ul>