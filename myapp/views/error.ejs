<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fehler</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1 class="text-danger">Fehler</h1>
        <h2>Status: <%= error.status || 500 %></h2>
        <p><strong>Nachricht:</strong> <%= typeof message !== 'undefined' ? message : 'Ein unbekannter Fehler ist aufgetreten.' %></p>
        <% if (error.stack) { %>
            <pre><%= error.stack %></pre>
        <% } %>
        <a href="/" class="btn btn-primary mt-3">Zurück zur Startseite</a>
    </div>
</body>
</html>